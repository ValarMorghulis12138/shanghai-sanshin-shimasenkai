/**
 * JSONBin.io Configuration (Fallback Only)
 * 
 * ⚠️  DO NOT USE THESE VALUES IN PRODUCTION!
 * 
 * This file only serves as a fallback and will display warnings if used.
 * Always configure proper environment variables:
 * 
 * Development:
 *   1. Copy .env.development.example to .env.development
 *   2. Fill in your development credentials
 * 
 * Production:
 *   1. Copy .env.production.example to .env.production
 *   2. Fill in your production credentials
 *   3. For GitHub Pages, add credentials as GitHub Secrets
 */

export const JSONBIN_CONFIG = {
  // ⚠️  Empty fallback - will trigger warnings
  SESSIONS_BIN_ID: '',
  REGISTRATIONS_BIN_ID: '',
  API_KEY: '',
  ADMIN_CONFIG_BIN_ID: ''
};

// Note: If these empty strings are used, the app will show an error message
// prompting you to configure environment variables properly
